<%= form_for(@acta_reunion) do |f| %>
  <% if @acta_reunion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acta_reunion.errors.count, "error") %> prohibited this acta_reunion from being saved:</h2>

      <ul>
      <% @acta_reunion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.date_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :correlativo %><br>
    <%= f.number_field :correlativo %>
  </div>
  <div class="field">
    <%= f.label :proyecto_id %><br>
    <%= f.text_field :proyecto_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

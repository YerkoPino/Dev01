<%= form_for(@usuario_elemento) do |f| %>
  <% if @usuario_elemento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usuario_elemento.errors.count, "error") %> prohibited this usuario_elemento from being saved:</h2>

      <ul>
      <% @usuario_elemento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :responsable %><br>
    <%= f.check_box :responsable %>
  </div>
  <div class="field">
    <%= f.label :elemento_id %><br>
    <%= f.text_field :elemento_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
